{
  "pluginAlias": "EnerTalkKm81",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "EnerTalk(Encored) 전력 미터를 HomeKit 으로 노출합니다(read-only). **클라우드 모드**(에너톡 이메일/비밀번호로 api2 조회) 또는 **로컬 모드**(기기를 클라우드 대신 이 호스트로 직접 받음, DNS 리다이렉트 필요)로 동작합니다. '실시간 전력'과 '당월 사용량'을 각각 켜고/끌 수 있고, 켜면 이름을 지정할 수 있습니다. 각 센서는 조도센서(lux) 로 홈 앱에 숫자가 뜨고, Eve 특성(W/kWh/V/A)+그래프도 함께 제공됩니다.",
  "footerDisplay": "로컬 모드: 라우터에서 eddie-ext.encoredtech.com 을 이 홈브릿지 호스트 IP 로 돌리면 기기가 클라우드 대신 여기로 붙습니다(클라우드가 죽어도 동작). 클라우드 모드: developer.enertalk.com 이 폐쇄돼도 auth2/api2 는 동작합니다.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "플러그인/로그 이름",
        "type": "string",
        "default": "EnerTalkKm81",
        "description": "Homebridge 로그와 Config UI X의 'Plugin Logs' 필터에 쓰이는 라벨입니다. 이 값이 로그 앞 [대괄호] 에 표시되고, Plugin Logs 는 이 이름으로 로그를 걸러 보여줍니다. 비우면 로그가 Plugin Logs 에서 안 보일 수 있습니다."
      },
      "email": {
        "title": "에너톡 이메일 (클라우드 모드)",
        "type": "string",
        "format": "email",
        "description": "클라우드 모드에 필요. 로컬 모드만 쓰면 비워도 됩니다(비우면 당월 사용량은 기기 누적 카운터 사용)."
      },
      "password": {
        "title": "에너톡 비밀번호 (클라우드 모드)",
        "type": "string",
        "x-schema-form": { "type": "password" }
      },
      "localMode": {
        "title": "로컬 모드 (기기 직수신)",
        "type": "boolean",
        "default": false,
        "description": "켜면 이 호스트가 :5010 TLS 서버를 열어 기기 데이터를 직접 받습니다. 라우터에서 eddie-ext.encoredtech.com → 이 호스트 IP 로 DNS 리다이렉트가 필요합니다. 클라우드 없이 실시간 전력이 동작합니다."
      },
      "localPort": {
        "title": "로컬 서버 포트",
        "type": "integer",
        "default": 5010,
        "minimum": 1,
        "maximum": 65535,
        "condition": { "functionBody": "return model.localMode === true;" },
        "description": "기기가 접속하는 포트(기본 5010). 보통 그대로 둡니다."
      },
      "meteringDay": {
        "title": "검침일 (일, 1~31)",
        "type": "integer",
        "minimum": 1,
        "maximum": 31,
        "condition": { "functionBody": "return model.localMode === true;" },
        "description": "당월 사용량은 기기 누적 카운터에서 이 날짜 기준으로 로컬 산출됩니다. 입력하면 이 값이 **최우선**(클라우드가 다른 검침일을 보고해도 덮어쓰지 않음). 비우면 클라우드 billing 에서 자동 학습."
      },
      "exposePower": {
        "title": "실시간 전력 액세서리 추가",
        "type": "boolean",
        "default": true,
        "description": "현재 소비전력(W)을 조도센서 액세서리로 노출."
      },
      "powerSensorName": {
        "title": "실시간 전력 센서 이름",
        "type": "string",
        "default": "실시간 전력",
        "condition": { "functionBody": "return model.exposePower !== false;" }
      },
      "exposeUsage": {
        "title": "당월 사용량 액세서리 추가",
        "type": "boolean",
        "default": true,
        "description": "검침일 기준 당월 누적(kWh)을 조도센서 액세서리로 노출."
      },
      "usageSensorName": {
        "title": "당월 사용량 센서 이름",
        "type": "string",
        "default": "당월 사용량",
        "condition": { "functionBody": "return model.exposeUsage !== false;" }
      },
      "pollingInterval": {
        "title": "실시간 폴링 주기(초)",
        "type": "integer",
        "default": 30,
        "minimum": 10,
        "maximum": 3600,
        "description": "실시간 W 조회 주기. 권장 30~60초."
      },
      "billingInterval": {
        "title": "당월 사용량 폴링 주기(초)",
        "type": "integer",
        "default": 300,
        "minimum": 60,
        "maximum": 86400,
        "description": "당월 kWh/요금 조회 주기. 권장 300초."
      },
      "exposeOutlet": {
        "title": "Eve 에너지 그래프용 콘센트 추가 (선택)",
        "type": "boolean",
        "default": false,
        "description": "Eve 에너지 그래프 UI 용 Outlet. On 스위치가 필수라 홈 앱에 토글이 보이지만 동작은 없습니다. 보통 꺼둡니다."
      },
      "outletName": {
        "title": "콘센트 액세서리 이름",
        "type": "string",
        "default": "소비전력",
        "condition": { "functionBody": "return model.exposeOutlet === true;" }
      }
    }
  }
}
